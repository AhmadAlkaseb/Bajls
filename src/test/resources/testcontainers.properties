# TestContainers configuration for Windows Docker Desktop
# Prioritize the NpipeSocket strategy which works on Windows with Docker Desktop
docker.client.strategy=org.testcontainers.dockerclient.NpipeSocketClientProviderStrategy

# Disable strategies that are failing
testcontainers.environment.provider.strategy.environmentFilePropertiesProvider=false
